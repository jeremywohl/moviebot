Features:
- normalize/liberalize command parameters
- more overwrite protection, probably by carrying (original, prefix and normalized) tuple around
- title case from the get go?
- separate web logging from debug; add timestamping to debug events (all lines); ts shell commands
- start working on longest track straight away
- be able to pause ripping and encoding queue, interrupt on deck
- handle mp4 failure, probably just by noticing absent resulting file
- command to show queues
- normalize tv episodes
- respond in the Slack channel that brung you
